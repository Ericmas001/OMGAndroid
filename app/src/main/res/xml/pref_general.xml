<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="gen_app_language"
        android:title="@string/pref_title_application_language"
        android:entries="@array/pref_application_languages_titles"
        android:entryValues="@array/pref_application_languages_values"
        android:defaultValue=""
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="gen_display_layout"
        android:title="@string/pref_title_display_layout"
        android:defaultValue="LIST"
        android:entries="@array/pref_display_layouts_titles"
        android:entryValues="@array/pref_display_layouts_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
